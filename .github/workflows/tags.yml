name: Test tags
on:
  push:
   tags:
    - v*
jobs:
    deploy:
      runs-on: ubuntu-18.04
      steps:
         - name: Checkout
           uses: actions/checkout@v2
         - name: Version
           run: echo $GITHUB_REF